# Check if CUVIS SDK is Available

Tests whether the Cubert CUVIS SDK library is linked and usable.

## Usage

``` r
cuvis_available()
```

## Value

Logical. `TRUE` if the SDK is available, `FALSE` otherwise.
