# Check if a Reference is Set

Check if a Reference is Set

## Usage

``` r
cuvis_has_reference(
  ctx,
  type = c("dark", "white", "white_dark", "sprad", "distance")
)
```

## Arguments

- ctx:

  A cuvis_processing_context object.

- type:

  Character. Reference type to check.

## Value

Logical.
